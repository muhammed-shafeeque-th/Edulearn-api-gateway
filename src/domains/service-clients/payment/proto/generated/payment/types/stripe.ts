// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v6.33.1
// source: payment/types/stripe.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';

export const protobufPackage = 'payment.stripe';

export interface StripePaymentSession {
  sessionId: string;
  publicKey: string;
  /** Stripe hosted checkout URL (optional) */
  url: string;
  /** Amount in the smallest unit (e.g., cents) */
  amount: number;
  /** I */
  currency: string;
}

export interface StripeVerifyRequest {
  sessionId: string;
}

export interface StripeVerifyResponse {
  paymentIntentId: string;
  verified: boolean;
}

function createBaseStripePaymentSession(): StripePaymentSession {
  return { sessionId: '', publicKey: '', url: '', amount: 0, currency: '' };
}

export const StripePaymentSession: MessageFns<StripePaymentSession> = {
  encode(
    message: StripePaymentSession,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (message.sessionId !== '') {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.publicKey !== '') {
      writer.uint32(18).string(message.publicKey);
    }
    if (message.url !== '') {
      writer.uint32(26).string(message.url);
    }
    if (message.amount !== 0) {
      writer.uint32(168).int64(message.amount);
    }
    if (message.currency !== '') {
      writer.uint32(178).string(message.currency);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number
  ): StripePaymentSession {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStripePaymentSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }

          message.amount = longToNumber(reader.int64());
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StripePaymentSession {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : '',
      publicKey: isSet(object.publicKey)
        ? globalThis.String(object.publicKey)
        : '',
      url: isSet(object.url) ? globalThis.String(object.url) : '',
      amount: isSet(object.amount) ? globalThis.Number(object.amount) : 0,
      currency: isSet(object.currency)
        ? globalThis.String(object.currency)
        : '',
    };
  },

  toJSON(message: StripePaymentSession): unknown {
    const obj: any = {};
    if (message.sessionId !== '') {
      obj.sessionId = message.sessionId;
    }
    if (message.publicKey !== '') {
      obj.publicKey = message.publicKey;
    }
    if (message.url !== '') {
      obj.url = message.url;
    }
    if (message.amount !== 0) {
      obj.amount = Math.round(message.amount);
    }
    if (message.currency !== '') {
      obj.currency = message.currency;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StripePaymentSession>, I>>(
    base?: I
  ): StripePaymentSession {
    return StripePaymentSession.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StripePaymentSession>, I>>(
    object: I
  ): StripePaymentSession {
    const message = createBaseStripePaymentSession();
    message.sessionId = object.sessionId ?? '';
    message.publicKey = object.publicKey ?? '';
    message.url = object.url ?? '';
    message.amount = object.amount ?? 0;
    message.currency = object.currency ?? '';
    return message;
  },
};

function createBaseStripeVerifyRequest(): StripeVerifyRequest {
  return { sessionId: '' };
}

export const StripeVerifyRequest: MessageFns<StripeVerifyRequest> = {
  encode(
    message: StripeVerifyRequest,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (message.sessionId !== '') {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number
  ): StripeVerifyRequest {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStripeVerifyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StripeVerifyRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : '',
    };
  },

  toJSON(message: StripeVerifyRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== '') {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StripeVerifyRequest>, I>>(
    base?: I
  ): StripeVerifyRequest {
    return StripeVerifyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StripeVerifyRequest>, I>>(
    object: I
  ): StripeVerifyRequest {
    const message = createBaseStripeVerifyRequest();
    message.sessionId = object.sessionId ?? '';
    return message;
  },
};

function createBaseStripeVerifyResponse(): StripeVerifyResponse {
  return { paymentIntentId: '', verified: false };
}

export const StripeVerifyResponse: MessageFns<StripeVerifyResponse> = {
  encode(
    message: StripeVerifyResponse,
    writer: BinaryWriter = new BinaryWriter()
  ): BinaryWriter {
    if (message.paymentIntentId !== '') {
      writer.uint32(10).string(message.paymentIntentId);
    }
    if (message.verified !== false) {
      writer.uint32(16).bool(message.verified);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number
  ): StripeVerifyResponse {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStripeVerifyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paymentIntentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.verified = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StripeVerifyResponse {
    return {
      paymentIntentId: isSet(object.paymentIntentId)
        ? globalThis.String(object.paymentIntentId)
        : '',
      verified: isSet(object.verified)
        ? globalThis.Boolean(object.verified)
        : false,
    };
  },

  toJSON(message: StripeVerifyResponse): unknown {
    const obj: any = {};
    if (message.paymentIntentId !== '') {
      obj.paymentIntentId = message.paymentIntentId;
    }
    if (message.verified !== false) {
      obj.verified = message.verified;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StripeVerifyResponse>, I>>(
    base?: I
  ): StripeVerifyResponse {
    return StripeVerifyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StripeVerifyResponse>, I>>(
    object: I
  ): StripeVerifyResponse {
    const message = createBaseStripeVerifyResponse();
    message.paymentIntentId = object.paymentIntentId ?? '';
    message.verified = object.verified ?? false;
    return message;
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER');
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER');
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
